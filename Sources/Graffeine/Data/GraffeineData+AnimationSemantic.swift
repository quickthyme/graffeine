import Foundation

extension GraffeineData {

    public enum AnimationSemantic: Hashable {
        case notAnimated
        case reload
        case next
        case previous
        case select
    }
}
